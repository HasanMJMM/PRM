import React from 'react';
import Layout from "../../layout/Layout";

function Member(props) {
    return (
        <Layout>
            This is Member page
        </Layout>
    );
}

export default Member;